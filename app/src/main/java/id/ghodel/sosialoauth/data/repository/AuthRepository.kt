package id.ghodel.sosialoauth.data.repository

import android.app.Application

class AuthRepository(application: Application) {

    companion object{
        private val TAG = AuthRepository::class.simpleName
    }
}